# ⚡ Быстрый старт

## За 5 минут к работающему серверу

### Шаг 1: Клонирование репозитория
```bash
git clone <этот репозиторий>
cd server-minecraft-minimal
```

### Шаг 2: Установка зависимостей

**Windows (MSYS2 MINGW64):**
```bash
pacman -Sy mingw-w64-x86_64-gcc mingw-w64-x86_64-make
# Затем запустите build.bat
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get install build-essential
```

**macOS:**
```bash
xcode-select --install
```

### Шаг 3: Сборка

```bash
make clean && make
```

Или просто:
```bash
bash build.sh        # На Linux/macOS
./build.bat          # На Windows
```

### Шаг 4: Запуск

```bash
./build/server
```

Вы должны увидеть:
```
╔════════════════════════════════════════╗
║  Optimized Minecraft Server v1.0       ║
║  Протокол 772 (1.21.8)                 ║
║  Для слабого железа (2GB RAM)          ║
╚════════════════════════════════════════╝

[SERVER] Инициализация сервера...
[SERVER] Максимум игроков: 1000
[SERVER] Частота тиков: 20 TPS
[SERVER] Дистанция рендера: 6 блоков
[SERVER] Сервер слушает на порту 25565
[TICK] Игровой цикл запущен (TPS=20)
[NETWORK] Сетевой поток запущен
[SERVER] Сервер успешно инициализирован
```

### Шаг 5: Подключение клиента

1. Откройте Minecraft Java Edition
2. Нажмите "Multiplayer"
3. "Add Server"
4. Адрес: `localhost:25565` (или IP вашего сервера)
5. Готово! ✅

## 🎮 Первые команды

```
/help           - Справка
/spawn          - Телепортация на спаун
/coords         - Показать координаты
```

## 🔧 Настройки

Отредактируйте `include/globals.h` перед сборкой:

```c
/* Максимум игроков */
#define MAX_PLAYERS 1000

/* Расстояние рендеринга */
#define RENDER_DISTANCE 6

/* Частота тиков (TPS) */
#define TICK_RATE 20
```

После изменения пересоберите:
```bash
make clean && make run
```

## 🐛 Ошибки и решения

### `gcc: command not found`
**Решение**: Установите GCC/build-essential (см. шаг 2)

### `Permission denied: './build/server'`
**Решение**: На Linux выполните `chmod +x build/server`

### `Address already in use`
**Решение**: Порт 25565 занят. Измените в `globals.h`:
```c
#define SERVER_PORT 25566
```

### Сервер падает сразу
**Решение**: Проверьте логи, запустите с дебагом:
```bash
make debug-run
```

## 📊 Мониторинг

Во время запуска сервер выводит статистику:
```
[TICK] Тик #400 | Игроков: 12 | Лаг-фреймов: 0
```

## 🚀 Оптимизация для вашего оборудования

### Слабое железо (512MB-1GB):
```c
#define MAX_PLAYERS 100
#define RENDER_DISTANCE 3
#define ENABLE_MOBS 0
```

### Стандартное (2GB):
```c
#define MAX_PLAYERS 1000
#define RENDER_DISTANCE 6
#define ENABLE_MOBS 1
```

### Мощное (8GB+):
```c
#define MAX_PLAYERS 5000
#define RENDER_DISTANCE 10
#define MOB_LIMIT 500
```

## 📝 Логирование

Включить детальное логирование:
```c
// В include/globals.h
#define DEBUG_LOG 1
#define DEBUG_MEMORY 1
```

Затем:
```bash
make debug-run
```

## 🎯 Что дальше?

1. **Проверьте производительность**: `make stats`
2. **Прочитайте документацию**: Откройте `OPTIMIZATION.md`
3. **Модифицируйте код**: Все в `src/` и `include/`
4. **Профилируйте**: `make profile`

## 💡 Советы

- **Для стабильного хостинга**: используйте Linux вместо Windows
- **Для максимума игроков**: используйте SSD вместо HDD
- **Для надёжности**: запускайте в `screen` или `tmux`:
  ```bash
  screen -S minecraft
  ./build/server
  # Отключиться: Ctrl+A, затем D
  # Вернуться: screen -r minecraft
  ```

---

**Готово!** Ваш сервер работает 🎉

Для более подробной информации смотрите `README.md`
