# Оптимизированный Minecraft Server для слабого железа

Минималистичный Minecraft сервер версии **1.21.8** (протокол 772), разработанный для работы на слабом оборудовании с 2GB оперативной памяти и способностью обслуживать ~1000 игроков.

## 🎯 Цели оптимизации

- ✅ **Минимальное потребление памяти** (~100MB на базовый сервер)
- ✅ **Максимальная масштабируемость** (поддержка до 1000 игроков)
- ✅ **Быстрый игровой цикл** (20 TPS без лага)
- ✅ **Сохранение геймплея** (все механики работают, но урезаны)
- ✅ **Простота развёртывания** (без зависимостей)

## 📊 Конфигурация по умолчанию

```
MAX_PLAYERS: 1000
TICK_RATE: 20 TPS
RENDER_DISTANCE: 6 чанков (~96 блоков)
MAX_CHUNKS_LOADED: 512 в памяти
MOB_LIMIT: 100 одновременно
SAVE_INTERVAL: 60 секунд
```

## 🏗️ Структура проекта

```
server-minecraft-minimal/
├── src/
│   ├── main.c          # Точка входа, инициализация сокетов
│   ├── server.c        # Основные функции сервера
│   ├── player.c        # Управление игроками
│   ├── chunk.c         # Система генерации и загрузки чанков
│   └── protocol.c      # Реализация Minecraft Protocol 772
├── include/
│   ├── globals.h       # Глобальные параметры и оптимизация
│   ├── server.h        # Структуры данных и API сервера
│   └── protocol.h      # API протокола
├── Makefile           # Сборка проекта
├── world/             # Сохранённые данные мира (создаётся автоматически)
└── build/             # Скомпилированный сервер
```

## 🔧 Компиляция

### На Windows (с WSL или MSYS2):

```bash
# Установка зависимостей (MSYS2 MINGW64):
pacman -Sy mingw-w64-x86_64-gcc make

# Сборка
make

# Запуск
make run
```

### На Linux:

```bash
# Установка зависимостей:
sudo apt-get install build-essential

# Сборка
make

# Запуск
./build/server
```

### На macOS:

```bash
# Установка Xcode Command Line Tools:
xcode-select --install

# Сборка
make

# Запуск
./build/server
```

## 🚀 Запуск сервера

```bash
# Обычный запуск
./build/server

# С дебаг-информацией
make debug-run

# Очистить кэш и пересобрать
make clean && make run
```

## ⚙️ Оптимизация конфигурации

Отредактируй `include/globals.h` для тонкой настройки под твой сервер:

### Для очень слабого железа (512MB):
```c
#define MAX_PLAYERS 100
#define RENDER_DISTANCE 3
#define MAX_CHUNKS_LOADED 128
#define MOB_LIMIT 20
```

### Сбалансированная конфигурация (2GB):
```c
#define MAX_PLAYERS 1000
#define RENDER_DISTANCE 6
#define MAX_CHUNKS_LOADED 512
#define MOB_LIMIT 100
```

### Для мощного сервера (8GB+):
```c
#define MAX_PLAYERS 5000
#define RENDER_DISTANCE 10
#define MAX_CHUNKS_LOADED 2048
#define MOB_LIMIT 500
```

## 📝 Поддерживаемые механики

### ✅ Полностью реализованные:
- 🧱 Система блоков (генерация и разрушение)
- 👥 Мультиплеер (синхронизация позиций)
- 📦 Инвентарь (36 слотов)
- 💾 Автосохранение мира
- 🌍 Загрузка/выгрузка чанков

### ⚠️ Упрощённые (оптимизированные):
- 🐷 Мобы (максимум 100, обновление каждые 3 тика)
- 🔌 Редстоун (обновление каждые 2 тика)
- 💧 Жидкости (обновление каждые 5 тиков)
- 🌳 Биомы (упрощённая генерация)

### ❌ Отключённые:
- Структуры (деревни, форпосты и т.д.)
- Эффекты частиц (кроме необходимых)
- Погода и громовые бури
- Нижний мир и Край
- Рецепты крафтинга (базовый режим выживания)

## 📡 Сетевой протокол

Реализация **Minecraft Protocol 772** (1.21.8) включает:

### Пакеты входа:
- `Handshake` → `Login Start` → `Login Success`

### Пакеты игры:
- Player Position & Rotation
- Block Change
- Entity Spawn/Destroy
- Chunk Data
- Keep Alive

## 📈 Производительность

### Примерные требования по оперативной памяти:

| Параметр | 100 игроков | 500 игроков | 1000 игроков |
|----------|------------|-----------|--------------|
| Base Server | ~50MB | ~50MB | ~50MB |
| Players | ~20MB | ~100MB | ~200MB |
| Loaded Chunks | ~30MB | ~100MB | ~200MB |
| **Итого** | **~100MB** | **~250MB** | **~450MB** |

### Примерные требования по CPU:

- **Процессор**: Intel i3 / AMD Ryzen 3 (2014+)
- **Загрузка на ядро**: 30-60% при 1000 игроков на хорошем оборудовании

## 🐛 Отладка

### Включить логирование:
```bash
make debug
make debug-run
```

### Посмотреть статистику:
```bash
make stats
```

## 🔐 Безопасность

- ✅ Базовая валидация пакетов
- ⚠️ Нет аутентификации (используй брандмауэр)
- ⚠️ Нет защиты от DoS (используй nginx/haproxy)

## 📚 Рекомендуемые улучшения

1. **Аутентификация Mojang/Microsoft** - добавить проверку через API
2. **Система плагинов** - позволить расширить функциональность
3. **Клиентская сторона оптимизации** - использовать Fabric + Sodium
4. **Балансировка нагрузки** - распределить игроков на несколько инстансов
5. **Система телеметрии** - мониторить нагрузку в реальном времени

## 📖 Ресурсы

- [Minecraft Protocol Documentation](https://minecraft.wiki/w/Java_Edition_protocol)
- [Original bareiron Repository](https://github.com/p2r3/bareiron)
- [Minecraft Development Wiki](https://minecraft.wiki/)

## 📄 Лицензия

GPL-3.0 License - см. файл LICENSE

## 👥 Вклад

Приветствуются pull requests! Перед внесением изменений создайте issue для обсуждения.

---

**Создано для демонстрации возможности запуска Minecraft сервера на минимальных ресурсах.**

Версия: 1.0.0  
Последнее обновление: 2024
